# Cassandra Cluster Deployment Configuration
# Copy this file to deploy-config.env and modify as needed

# Basic Configuration
CLUSTER_NAME=cassandra-cluster
ENVIRONMENT=dev
NODE_COUNT=3
INSTANCE_TYPE=m5.large
AWS_REGION=us-east-1

# Deployment Options
AUTO_APPROVE=false
ENABLE_MONITORING=true
ASSIGN_ELASTIC_IPS=true

# Storage Configuration (will be auto-sized based on environment)
# CUSTOM_DATA_VOLUME_SIZE=100  # Uncomment to override auto-sizing

# Network Configuration (will be auto-generated based on environment)
# CUSTOM_VPC_CIDR=10.0.0.0/16  # Uncomment to override auto-generation

# Advanced Configuration
TERRAFORM_PARALLELISM=10
BACKUP_ENABLED=true
ENCRYPTION_AT_REST=true

# SSH Configuration
SSH_KEY_NAME=cassandra-cluster-key

# Environment-specific overrides
# These will be applied automatically based on ENVIRONMENT value:
#
# Development (dev):
#   - VPC CIDR: 10.30.0.0/16
#   - Data Volume: 100GB
#   - SSH Access: Open (0.0.0.0/0)
#   - Encryption in Transit: Disabled
#
# Staging (staging):
#   - VPC CIDR: 10.20.0.0/16
#   - Data Volume: 200GB
#   - SSH Access: Open (0.0.0.0/0)
#   - Encryption in Transit: Disabled
#
# Production (prod):
#   - VPC CIDR: 10.10.0.0/16
#   - Data Volume: 500GB
#   - SSH Access: Restricted (10.0.0.0/8)
#   - Encryption in Transit: Enabled